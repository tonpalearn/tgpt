import { SlidesViewer } from "@/components/present/SlidesViewer";

export const metadata = {
  title: "Stage 0 Review · ThailandGPT · For Patrick",
  description: "Internal presentation deck — mock data, internal review only.",
  robots: { index: false, follow: false },
};

export default function PresentPage() {
  return <SlidesViewer />;
}
